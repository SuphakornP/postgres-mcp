# PostgreSQL Database Connection URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/mydb

# API Key for MCP Server Security
# Clients must provide this key via X-API-Key header or Authorization: Bearer <key>
# Leave empty to disable API key validation (not recommended for production)
MCP_API_KEY=your-secure-api-key-here

# AWS Credentials (for deployment to Bedrock AgentCore)
# Note: For deployment, configure credentials in .aws/credentials file instead
# Temporary credentials (ASIA prefix) require AWS_SESSION_TOKEN
AWS_ACCESS_KEY_ID=xxx
AWS_SECRET_ACCESS_KEY=xxx
AWS_SESSION_TOKEN=xxx  # Required for temporary credentials
AWS_REGION=us-west-2

# AWS Profile Configuration
# Create .aws/credentials with your profile:
# [YOUR_PROFILE_NAME]
# aws_access_key_id=YOUR_KEY
# aws_secret_access_key=YOUR_SECRET
# aws_session_token=YOUR_TOKEN
#
# Create .aws/config with:
# [profile YOUR_PROFILE_NAME]
# region = us-west-2
# output = json